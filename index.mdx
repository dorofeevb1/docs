<!-- sidebar.component.html -->
<aside class="sidebar" [ngClass]="{'sidebar--collapsed': isCollapsed}" role="navigation" aria-label="Main navigation">
  <!-- Hamburger button for mobile -->
  <button class="sidebar__hamburger" (click)="toggleMenu()" aria-label="Toggle menu" aria-expanded="{{!isCollapsed}}" *ngIf="isMobile">
    <span class="sidebar__hamburger-icon material-icons">menu</span>
  </button>

  <!-- Sidebar content -->
  <div class="sidebar__content" [@slideInOut]="isCollapsed ? 'out' : 'in'">
    <!-- Logo -->
    <div class="sidebar__logo">
      <svg width="200" height="50" aria-label="ВНГРФ Logo">
        <rect width="200" height="50" fill="#9B1D64"/>
        <text x="10" y="35" fill="#FFFFFF" font-family="Montserrat, sans-serif" font-weight="700" font-size="24">ВНГРФ</text>
      </svg>
    </div>

    <!-- Navigation -->
    <nav class="sidebar__nav">
      <ul class="sidebar__nav-list">
        <li class="sidebar__nav-item" routerLinkActive="sidebar__nav-item--active">
          <a class="sidebar__nav-link" routerLink="/news" aria-label="News Feed">
            <span class="material-icons sidebar__nav-icon">article</span>
            <span class="sidebar__nav-text">Лента</span>
          </a>
        </li>
        <li class="sidebar__nav-item" routerLinkActive="sidebar__nav-item--active">
          <a class="sidebar__nav-link" routerLink="/team" aria-label="Our Team">
            <span class="material-icons sidebar__nav-icon">groups</span>
            <span class="sidebar__nav-text">Наши люди</span>
          </a>
        </li>
        <li class="sidebar__nav-item" routerLinkActive="sidebar__nav-item--active">
          <a class="sidebar__nav-link" routerLink="/about" aria-label="About Us">
            <span class="material-icons sidebar__nav-icon">info</span>
            <span class="sidebar__nav-text">О нас</span>
          </a>
        </li>
        <li class="sidebar__nav-item" routerLinkActive="sidebar__nav-item--active" *ngIf="isAdmin">
          <a class="sidebar__nav-link" routerLink="/admin" aria-label="Admin Panel">
            <span class="material-icons sidebar__nav-icon">settings</span>
            <span class="sidebar__nav-text">Админ-панель</span>
          </a>
        </li>
        <li class="sidebar__nav-item" routerLinkActive="sidebar__nav-item--active">
          <a class="sidebar__nav-link" (click)="handleAuth()" aria-label="{{isLoggedIn ? 'Logout' : 'Login'}}">
            <span class="material-icons sidebar__nav-icon">{{isLoggedIn ? 'logout' : 'lock'}}</span>
            <span class="sidebar__nav-text">{{isLoggedIn ? 'Выход' : 'Логин'}}</span>
          </a>
        </li>
      </ul>
    </nav>
  </div>
</aside>